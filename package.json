{
  "name": "songcourt",
  "version": "1.0.0",
  "description": "SongCourt backend API for Spotify OAuth and music snapshot normalization.",
  "scripts": {
    "check": "find api -type f -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "engines": {
    "node": ">=20"
  },
  "private": true
}
